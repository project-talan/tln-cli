module.exports = {
  tags: (tln) => [''],
  options: (tln, options) => {},
  dotenvs: (tln) => [],
  depends: (tln) => [],
  inherits: (tln) => [],
  variables: (tln, variables) => {},
  steps: (tln) => [
    {
      id: 'install',
      desc: '',
      filter: 'linux',
      script: (tln, script) => {
        // script.set(['printenv'])
      }
    }
  ],
  components: (tln) => []
}
