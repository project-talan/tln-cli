
module.exports = {
  tags: async (tln) => [],
  options: async (tln) => [],
  dotenvs: async (tln) => [],
  inherits: async (tln) => [],
  env: async (tln, env) => {},
  steps: async (tln) => [],
  components: async (tln) => require('./components.js')
}
